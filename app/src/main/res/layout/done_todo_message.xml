<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center_vertical">

    <TextView
        android:id="@+id/done_task_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:text="Hello"
        android:textSize="20sp" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@android:drawable/ic_delete"
        android:layout_alignBottom="@+id/add_back_btn_id"
        android:layout_alignParentEnd="true"
        android:id="@+id/done_task_delete"
        android:onClick="removeTask" />

    <Button
        android:text="Add Back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/add_back_btn_id"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/done_task_delete"
        android:onClick="addTaskBackToStillTodoList" />

</RelativeLayout>